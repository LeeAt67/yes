# [DIRECTORY NAME]

> 📅 生成日期：[DATE]  
> 📂 路径：`[DIRECTORY PATH]`  
> 🏷️ 类型：**Generic Component** (通用组件)

---

## 概览

描述当前通用组件的主要职责和功能。

模板示例：

这是[组件名称]通用组件，主要负责[主要功能描述]。该组件是一个展示型或交互型的可复用单元，不包含业务逻辑，依赖父组件传入数据和回调，可在多个页面/模块中复用。

## 相关概念

核心概念名词解释，口径统一。

模板示例：

- [概念1]: [概念1的详细解释]，格式定义为 type.ts 中的 [类型定义]
- [概念2]: [概念2的详细解释]
- [概念3]: [概念3的详细解释]

## Props 定义

**定义位置**：`[文件路径]`

**主要 Props**：

- `[propName1]`: [propType] - [prop说明]，由父组件传入
- `[propName2]`: [propType] - [prop说明]，由父组件传入
- `[callback]`: (data) => void - [回调说明]，触发时通知父组件
- `[propName3]`: (可选) [propType] - [可选prop说明]

## 数据结构

**类型定义位置**：`[文件路径]`

**主要类型**：

- `[类型名1]`: [类型描述]，定义在 `[文件路径]` 中的 `[类型定义]`
- `[类型名2]`: [类型描述]，定义在 `[文件路径]` 中的 `[类型定义]`
- `[类型名3]`: [类型描述]，定义在 `[文件路径]` 中的 `[类型定义]`

## 文件结构

描述组件目录的文件/文件夹结构及其职责。

模板示例：

```
[ComponentName]/
├── index.jsx           # 组件入口，导出主组件
├── [ComponentName].jsx # 组件主实现
├── hooks/              # 组件专用 Hooks
│   └── use[HookName].js
├── utils/              # 工具函数
│   └── [utilName].js
└── type.ts             # TypeScript 类型定义
```

### 核心文件

- `[ComponentName].jsx`：组件主实现，负责[核心展示/交互]
- `index.jsx`：导出入口，暴露组件和相关类型

### 辅助文件

- `type.ts`：组件 Props 和内部状态的类型定义
- `hooks/use[HookName].js`：组件专用 Hook，负责[Hook功能]
- `utils/[utilName].js`：工具函数，负责[工具功能]

## 交互和事件

描述组件对用户交互的响应、触发的事件。无业务逻辑，仅通知父组件。

模板示例：

### 事件回调

- `on[Event1]`：[事件1描述]，传递 [参数] 给父组件处理
- `on[Event2]`：[事件2描述]，用于通知父组件 [信息]

### 交互流程

```
用户操作（如点击）
↓ 触发 [事件处理函数]
↓ 调用 on[Callback] 回调
↓ 父组件接收并处理业务逻辑
↓ 父组件可能更新状态，重新渲染
```

## 复用说明

描述该组件在项目中的使用场景和复用情况。由于是通用组件，应支持多种业务场景。

模板示例：

- **复用模块**：在 [模块1]、[模块2] 等模块中使用
- **复用页面**：在 [页面1]、[页面2] 等页面中使用
- **使用频率**：[高/中/低] - 共 [N] 处使用

**设计原则**：组件本身不关心数据的业务含义，只负责展示和交互，具体业务逻辑由使用方决定。

## 相关依赖

描述该组件依赖的外部库和内部模块。通常应较少依赖其他业务模块。

## 关联文档

描述和当前目录有关系的相关联文档的位置。

模板示例：

- [相关文档1.md](./相关文档1.md) - [文档1的说明]
- [相关文档2.md](./相关文档2.md) - [文档2的说明]

## 技术栈

描述当前目录使用的核心技术库和工具。

模板示例：

- **UI 库**: React [VERSION]
- **样式**: [CSS方案]
- **其他依赖**: [其他技术]

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
