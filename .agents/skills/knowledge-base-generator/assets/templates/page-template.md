# [DIRECTORY NAME]

> 📅 生成日期：[DATE]  
> 📂 路径：`[DIRECTORY PATH]`  
> 🏷️ 类型：**Page** (页面)

---

## 概览

描述当前页面的主要职责和功能。

模板示例：

这个页面是[页面名称]页面，主要负责[页面功能描述]。该页面提供[主要功能点1]、[主要功能点2]等功能。

## 测试用例覆盖

> 🔍 **验证规则**: 必须到 `e2e/tests/quality/` 目录下检查是否存在相关用例，严禁凭空编造。

描述页面相关的测试用例覆盖情况。

模板示例：

- [ ] 页面渲染测试 (`e2e/tests/quality/...`)
- [ ] 数据加载测试
- [ ] 用户交互测试
- [ ] 路由跳转测试
- [ ] 边界情况测试

## 路由信息

描述页面的路由配置信息。

模板示例：

- **路由路径**: [完整路由路径]（支持参数：[参数1]、[参数2]）
- **页面ID**: [页面ID]
- **所属模块**: [模块名称]
- **权限要求**: [权限描述或"无特殊要求"]
- **菜单显示**: [是否显示在菜单中]

## 数据结构

描述页面的数据结构和数据流转。

模板示例：

### [数据结构1]

[数据结构1的描述]，格式参考 [类型定义文件] 中的 [类型定义]。

### [数据结构2]

[数据结构2的描述]，数据从 [数据源] 获取，通过 [转换函数/方法] 转换为 [目标格式]。

## 页面状态和数据

描述页面使用的状态管理和数据流。

模板示例：

### 全局状态使用

- [全局状态名称]：用于 [用途描述]

### 页面级状态

- [页面状态1]：用于 [用途描述]
- [页面状态2]：用于 [用途描述]

### 数据流程

```
页面加载
↓ 获取 URL 参数：[参数名称]
↓ 从 [全局状态/API] 获取初始数据
↓ 初始化 [页面状态]
↓ 渲染 [主容器组件]
```

## 子组件

列出页面使用的子组件及其职责。

模板示例：

### 核心容器组件

- `[Component1]`：负责[组件1功能描述]
- `[Component2]`：负责[组件2功能描述]

### 业务功能组件

- `[Component3]`：负责[组件3功能描述]

## 页面文件结构

描述当前页面的目录结构和文件职责。

模板示例：

```
[页面目录]/
├── index.jsx          # 页面入口，导出主组件
├── [PageName].jsx     # 页面主组件
├── components/        # 页面专用组件
│   ├── [Component1]/  # 组件1：[功能]
│   └── [Component2]/  # 组件2：[功能]
├── hooks/             # 页面专用 Hooks
│   └── use[HookName].js
├── utils/             # 页面工具函数
│   └── [utilName].js
└── type.ts            # 页面相关类型定义
```

### 主要文件说明

- `[PageName].jsx`：页面主容器组件，负责 [主要职责]
- `index.jsx`：页面入口，导出页面组件
- `components/`：页面专用组件
- `hooks/use[HookName].js`：页面级自定义 Hook，负责 [Hook功能]
- `utils/[utilName].js`：页面工具函数，负责 [工具功能]

## 页面流程

描述页面的主要交互流程和数据流转。

模板示例：

### 初始化流程

```
页面挂载
↓ 执行 useEffect 初始化逻辑
↓ 从 [数据源] 获取数据
↓ 更新 [页面状态]
↓ 渲染主容器
```

### 用户交互流程

```
用户在 [功能1] 进行操作
↓ 触发 [事件处理器]
↓ 更新 [页面状态] 或调用 [API]
↓ [子组件] 响应状态变化
↓ 显示 [结果/反馈]
↓ 渲染 [子组件2]
↓ [用户操作] → [状态更新]
↓ 重新渲染
```

## 路由配置

描述页面的路由配置信息。

模板示例：

- **路由路径**: `/path/to/page`
- **路由参数**: [参数说明]
- **权限要求**: [权限说明]

## 关联文档

描述和当前页面有关系的相关联文档的位置。

模板示例：

- [使用的组件](../components/ComponentName/Agents.md) - 组件文档
- [使用的状态](../store/StateName/Agents.md) - 状态文档
- [API 文档](../../api/PageName.md) - API 文档

## 技术栈

描述当前页面使用的核心技术库和工具，重点关注页面特定的技术依赖。

模板示例：

- **路由库**: React Router [VERSION] / Next.js Router
- **数据获取**: SWR [VERSION] / React Query [VERSION]
- **其他依赖**: [页面特定的其他重要技术]

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
