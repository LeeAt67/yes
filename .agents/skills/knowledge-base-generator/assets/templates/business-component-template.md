# [DIRECTORY NAME]

> 📅 生成日期：[DATE]  
> 📂 路径：`[DIRECTORY PATH]`  
> 🏷️ 类型：**Business Component** (业务组件)

---

## 概览

描述当前业务组件的核心职责、所关联的业务模块及功能。

模板示例：

这是[组件名称]业务组件，主要负责[主要功能描述]。该组件集成了来自[模块1]、[模块2]等多个业务模块的功能，封装了[具体业务逻辑1]、[具体业务逻辑2]等业务逻辑，可在多个页面中复用。

## 测试用例覆盖

> 🔍 **验证规则**: 必须到 `e2e/tests/quality/` 目录下检查是否存在相关用例，严禁凭空编造。

模板示例：

- [ ] 核心功能渲染测试 (`e2e/tests/quality/...`)
- [ ] 业务逻辑交互测试
- [ ] 边界情况测试
- [ ] 错误处理测试

## 相关概念

核心业务概念及术语解释，与产品功能对应。

模板示例：

- [业务概念1]: [业务概念的详细解释]，在系统中的作用是[作用说明]，格式定义为 type.ts 中的 [类型定义]
- [业务概念2]: [业务概念的详细解释]
- [业务概念3]: [业务概念的详细解释]

## Props 定义

**定义位置**：`[文件路径]`

**主要 Props**：

- `[moduleConfig]`: [configType] - 来自[模块名]的配置信息
- `[onAction]`: (action: string) => void - 业务事件回调
- `[propName2]`: [propType] - [prop说明]
- `[propName3]`: (可选) [propType] - [可选prop说明]

## 渲染逻辑

描述组件封装的业务逻辑、决策流程、渲染性能优化以及与后端 API 的交互。

模板示例：

### [业务流程1]

当[触发条件]时，组件执行以下步骤：

1. [步骤1]：调用 [API/方法名]，获取[数据类型]
2. [步骤2]：根据[条件]进行判断，选择[分支1]/[分支2]
3. [步骤3]：调用相应的业务接口（如[接口名]）

涉及的状态：`[state1]`、`[state2]`

### [业务流程2]

[业务流程2的描述]

## 数据结构

描述组件内部的数据结构和状态管理。

**类型定义位置**：`[文件路径]`

**主要类型**：

- `[类型名1]`: [类型描述]，定义在 `[文件路径]` 中的 `[类型定义]`
- `[类型名2]`: [类型描述]，定义在 `[文件路径]` 中的 `[类型定义]`
- `[类型名3]`: [类型描述]，定义在 `[文件路径]` 中的 `[类型定义]`

### 状态管理

模板示例：

**[状态1]**

- **类型**：`[状态类型]`（定义在 `[文件路径]` 中的 `[类型定义]`）
- **来源**：[来源说明，如：从 Props 获取 / API 调用返回 / 组件内部初始化]
- **用途**：[状态的作用和用途]
- **更新时机**：[何时更新该状态]

**[状态2]**

- **类型**：`[状态类型]`（定义在 `[文件路径]` 中的 `[类型定义]`）
- **来源**：[来源说明]
- **用途**：[状态的作用和用途]
- **更新时机**：[何时更新该状态]

## API 和服务

描述组件调用的 API 和服务。

模板示例：

### [API名称1]

- **接口**：`POST /api/[endpoint]`
- **用途**：[用途说明]
- **请求参数**：[参数描述]
- **响应数据**：[响应数据内容描述]
- **调用位置**：[调用接口的函数/方法名]

### [API名称2]

[API描述]

## 文件结构

描述组件目录的文件/文件夹结构及其职责。

模板示例：

```
[DIRECTORY NAME]/
├── index.ts              # 组件入口，导出组件及类型
├── [ComponentName].tsx   # 主组件文件，包含业务逻辑
├── types.ts              # TypeScript 类型定义
├── hooks/
│   ├── use[HookName1].ts # [钩子功能说明]
│   └── use[HookName2].ts # [钩子功能说明]
├── services/
│   └── [serviceName].ts  # 调用具体业务接口的服务封装
└── utils/
    └── [utilName].ts     # 组件内工具函数
```

## 交互和事件

描述组件对用户交互的响应、触发的事件及业务影响。

模板示例：

- **[交互1]**：用户[操作]时，组件[响应]，并触发[回调函数名]，最终[业务结果]
- **[交互2]**：[交互描述]

## 复用说明

描述组件在哪些页面/模块中被复用、支持的业务配置和扩展点。

模板示例：

- 在[页面1]中用于[用途1]
- 在[页面2]中用于[用途2]
- 在[模块1]中用于[用途3]

**复用方式**：通过 Props 中的 `[moduleConfig]` 配置来支持不同模块场景，不需修改组件代码。

**扩展点**：如需支持新的模块或业务流程，可通过 [扩展方式] 进行扩展。

## 依赖关系

描述组件依赖的其他模块和组件。

模板示例：

| 依赖      | 用途       | 位置       |
| --------- | ---------- | ---------- |
| [模块1]   | [用途说明] | [导入路径] |
| [组件1]   | [用途说明] | [导入路径] |
| [API服务] | [用途说明] | [导入路径] |

## 关联文档

- [外部设计文档](URL)
- [后端接口定义](URL)
- [相关组件 AGENTS.md](路径)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
