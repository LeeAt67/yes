# [DIRECTORY NAME]

> 📅 生成日期：[DATE]  
> 📂 路径：`[DIRECTORY PATH]`  
> 🏷️ 类型：**State** (状态管理)

---

## 概览

描述当前状态的主要职责和数据管理范围。

模板示例：

这是[状态名称]状态管理器（MobX Store），主要负责[数据管理功能描述]。该状态管理[数据类型1]、[数据类型2]等业务数据，供 [使用范围] 使用。

## 作用域

描述该状态的作用范围。

模板示例：

- **作用域**: [global/module/page] - [作用域说明]
- **所属模块**: [模块名称]（如果是模块级状态）
- **定义文件**: [文件路径]
- **使用方式**: [useContext/import/其他方式]

## Observable 字段

**定义位置**：`[文件路径]`

**主要字段**：

- `[fieldName1]`: [类型] - [字段功能和使用说明]
- `[fieldName2]`: [类型] - [字段功能和使用说明]
- `[fieldName3]`: (可选) [类型] - [可选字段说明]

## Computed 方法

**定义位置**：`[文件路径]`

**计算属性**：

- `[computedName1]`: [返回类型] - 返回 [返回值说明]，用于 [用途]
- `[computedName2]`: [返回类型] - 返回 [返回值说明]，用于 [用途]

## 数据结构

**类型定义位置**：`[文件路径]`

**主要结构**：

- `[field1]`: [type] - [字段说明]
- `[field2]`: [type] - [字段说明]
- `[nested]`: (可选) 嵌套结构，包含 [nestedField] 等字段

## Action 方法

**定义位置**：`[文件路径]`

**主要方法**：

### [方法1名称]

- **功能**：[方法功能描述]
- **参数**：`param1` ([类型])、`param2` ([类型])
- **返回**：[返回类型] - [返回值说明]

### [方法2名称]

- **功能**：[方法功能描述] (异步)
- **参数**：`param` ([类型])
- **返回**：Promise<[返回类型]> - [异步说明]

## 数据输入来源

描述状态数据的输入来源和初始化方式。

模板示例：

- **初始化**: 从 [数据源] 初始化，通过 [初始化方法]
- **数据获取**: 通过 `[方法名]` 从 [服务/API] 获取
- **数据更新**: 通过 `[方法名]` 或 `[Action方法]` 更新
- **用户操作**: 通过 `[Action方法]` 触发数据变更
- **事件订阅**: [事件类型] 触发数据更新

## 数据消费和使用

描述状态数据在哪些场景下被读取和使用。

模板示例：

### 在页面中的使用

- **[页面名称]** 页面使用 `[字段名]` 用于 [用途描述]
- **[页面名称]** 页面使用 `[计算属性]` 用于 [用途描述]

### 在组件中的使用

- **[组件名称]** 组件使用 `[字段名]` 用于 [用途描述]
- **[组件名称]** 组件调用 `[方法名]` 用于 [操作描述]

### 跨状态依赖

- 依赖 `[其他状态名称]` 的 [字段/方法]
- 被 `[其他状态名称]` 依赖

## 文件结构

描述该状态相关的文件组织。

模板示例：

```
[状态文件所在目录]/
├── [StateName].js       # 状态定义文件
├── [StateName].ts       # 类型定义（如果分离）
└── [relatedFile].js     # 相关辅助文件（如果有）
```

描述状态相关的测试用例覆盖情况。

模板示例：

- [ ] 状态初始化测试
- [ ] 数据更新测试
- [ ] 数据读取测试
- [ ] 边界情况测试

## 目录结构

描述当前状态相关的文件结构。

模板示例：

- `[StateName].js`：状态定义文件，包含状态类/函数和所有方法
- `types.ts`：状态相关的 TypeScript 类型定义
- `utils.js`：状态相关的工具函数

## 测试用例覆盖

> 🔍 **验证规则**: 必须到 `e2e/tests/quality/` 目录下检查是否存在相关用例，严禁凭空编造。

模板示例：

- [ ] 状态变更单元测试
- [ ] 异步数据水合测试 (`e2e/tests/quality/...`)

## 关联文档

描述和当前状态有关系的相关联文档的位置。

模板示例：

- [使用该状态的组件](../components/ComponentName/Agents.md) - 组件文档
- [使用该状态的页面](../pages/PageName/Agents.md) - 页面文档

## 技术栈

描述当前状态使用的核心技术库和工具。

模板示例：

- **状态管理库**: MobX [VERSION] / Redux [VERSION] / Context API
- **其他依赖**: [状态特定的其他重要技术]

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
